- content_for :express_nav do
  %p= link_to t('admin.archive_numbers.show.title'), archive_number_path( @archive )
  %p= link_to t('admin.archive_numbers.index.title'), archive_numbers_path

%h1= t('.title')
= render 'form'
